Lab1: xv6 & Unix Utilities

- 管道为什么要设置两个？
- 为什么读写完要关闭端口？如果不关闭会有什么问题？
- 如果只有一个管道会有什么问题？什么情况下可以只使用一个管道？

Lab2: Process & System Call

- 陷入内核或回到用户态时，上下文在哪里，如何切换？保存在了哪里？
- 系统调用的过程和调用链

Lab3: Lock

- 死锁是怎么发生的？结合指导书，如何避免死锁？
- 你的代码中如何规避死锁？
- 哈希表是否使用了全局锁？为什么要使用全局锁（或桶锁）？
- 桶空间不足后如何寻找下一个桶？顺序是什么？

Lab4: Page Table

- 如何同步内核页表和用户页表？潜在问题？
- 如何确定页表项是否存在？
- `SV39` 的 `39` 是什么意思？
- 用户的内核页表何时创建？怎么创建？怎么同步？
- 共享区域如何共享？怎么释放？

Lab5: EXT2 based on FUSE

- 增加逻辑：
  - 创建目录和/或文件时，增加固定前缀和/或后缀
  - 实现最多只能有 `n` 个目录和/或文件
  - `ls` 最多显示 `n` 个目录和/或文件
  - 实现显示、修改目录和/或文件权限的功能
  - 实现创建目录同时在同目录下创建一个文件的功能
- 你的文件系统与 `simplefs` 有什么区别？讲一下结构体的设计细节。
